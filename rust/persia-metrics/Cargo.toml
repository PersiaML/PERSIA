[package]
authors = ["Kuaishou AI Platform PersiaML Team <zhuxuefeng@mkuaishou.com>"]
edition = "2018"
name = "persia-metrics"
version = "0.1.0"
license = "MIT"

[dependencies]
persia-embedding-config = {path = "../persia-embedding-config"}
persia-scheduled-thread-pool = {path = "../persia-scheduled-thread-pool"}
once_cell = "1.4"
prometheus = {version = "0.12.0", features = ["push"]}
prometheus_exporter = "0.8.2"
thiserror = "1.0"
tracing = "0.1"
